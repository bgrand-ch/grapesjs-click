export const pluginName = 'grapesjs-click'

export const logScope = '[GrapesJS::ClickPlugin]'

const commandPrefix = 'click'

export const grabBlockCommand = `${commandPrefix}:grab-block`
export const dropBlockCommand = `${commandPrefix}:drop-block`

export const grabComponentCommand = `${commandPrefix}:grab-component`
export const dropComponentCommand = `${commandPrefix}:drop-component`
